header.header
    .header__column
        a(href=routes.home)
           i.fab.fa-youtube
    .header__column
            form(action=routes.search, method="get", name="term")
                input(type="text", placeholder="Search by term...")

        ul
            li  
                a(href=routes.join) Join
            li
                a(href=routes.login) Log In

